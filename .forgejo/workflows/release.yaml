name: Release

on:
  push:
    tags: ["v*"]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"

jobs:
  version:
    name: Resolve Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve version
        id: version
        uses: https://forge.blackleafdigital.com/Public/actions/resolve-version@main
        with:
          tag-prefix: v
          bump: patch

  test-llm-smoke:
    name: LLM Smoke Test
    runs-on: ubuntu-latest
    needs: version
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: https://forge.blackleafdigital.com/Public/actions/setup-rust@main
        with:
          registries: '[{"name": "forgejo", "index": "sparse+https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/cargo/", "token": "${{ secrets.FORGEJO_ACTIONS_TOKEN }}"}]'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Restore cargo cache
        uses: https://forge.blackleafdigital.com/Public/actions/s3-cache@main
        with:
          mode: restore
          key: cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          path: ${{ env.CARGO_HOME }}/registry ${{ env.CARGO_HOME }}/git
          bucket: ${{ secrets.S3_BUCKET }}
          endpoint: https://${{ secrets.S3_ENDPOINT }}
          region: ${{ secrets.S3_REGION }}
          access-key: ${{ secrets.S3_ACCESS_KEY }}
          secret-key: ${{ secrets.S3_ACCESS_SECRET }}

      - name: Install uv
        run: pip install uv

      - name: Build Python bindings
        run: |
          uv pip install --system maturin pytest pytest-asyncio
          maturin develop -m crates/blazen-py/Cargo.toml --release

      - name: Setup pnpm
        run: corepack enable && corepack prepare pnpm@latest --activate

      - name: Build Node bindings
        run: |
          cd crates/blazen-node
          pnpm install
          pnpm run build

      - name: Run Python LLM smoke tests
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: pytest tests/python/test_llm_smoke.py -v

      - name: Run Node LLM smoke tests
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: node --test tests/node/test_llm_smoke.mjs

      - name: Save cargo cache
        if: always()
        uses: https://forge.blackleafdigital.com/Public/actions/s3-cache@main
        with:
          mode: save
          key: cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          path: ${{ env.CARGO_HOME }}/registry ${{ env.CARGO_HOME }}/git
          bucket: ${{ secrets.S3_BUCKET }}
          endpoint: https://${{ secrets.S3_ENDPOINT }}
          region: ${{ secrets.S3_REGION }}
          access-key: ${{ secrets.S3_ACCESS_KEY }}
          secret-key: ${{ secrets.S3_ACCESS_SECRET }}

  publish-rust:
    name: Publish Rust Crates
    runs-on: ubuntu-latest
    needs: [version, test-llm-smoke]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check step status
        id: check
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: check
          version: ${{ needs.version.outputs.version }}
          step: publish-rust
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

      - name: Setup Rust
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/setup-rust@main
        with:
          registries: '[{"name": "forgejo", "index": "sparse+https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/cargo/", "token": "${{ secrets.FORGEJO_ACTIONS_TOKEN }}"}]'

      - name: Restore cargo cache
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/s3-cache@main
        with:
          mode: restore
          key: cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          path: ${{ env.CARGO_HOME }}/registry ${{ env.CARGO_HOME }}/git
          bucket: ${{ secrets.S3_BUCKET }}
          endpoint: https://${{ secrets.S3_ENDPOINT }}
          region: ${{ secrets.S3_REGION }}
          access-key: ${{ secrets.S3_ACCESS_KEY }}
          secret-key: ${{ secrets.S3_ACCESS_SECRET }}

      - name: Stamp versions
        if: steps.check.outputs.completed != 'true'
        run: |
          VERSION="${{ needs.version.outputs.version }}"
          echo "Stamping all crate versions with $VERSION"
          # Update workspace version
          sed -i "s/^version = \"0.1.0\"/version = \"$VERSION\"/" Cargo.toml
          # Update internal dependency versions
          sed -i "s/blazen-events = { version = \"0.1.0\"/blazen-events = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen-macros = { version = \"0.1.0\"/blazen-macros = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen-core = { version = \"0.1.0\"/blazen-core = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen-llm = { version = \"0.1.0\"/blazen-llm = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen-persist = { version = \"0.1.0\"/blazen-persist = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen-pipeline = { version = \"0.1.0\"/blazen-pipeline = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen-prompts = { version = \"0.1.0\"/blazen-prompts = { version = \"$VERSION\"/" Cargo.toml
          sed -i "s/blazen = { version = \"0.1.0\"/blazen = { version = \"$VERSION\"/" Cargo.toml

      - name: Publish crates in dependency order
        if: steps.check.outputs.completed != 'true'
        run: |
          # Publish in dependency order with --no-verify (workspace deps are local/forgejo, not crates.io)
          cargo publish -p blazen-events --registry forgejo --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-macros --registry forgejo --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-persist --registry forgejo --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-core --registry forgejo --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-llm --registry forgejo --all-features --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-prompts --registry forgejo --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-pipeline --registry forgejo --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen --registry forgejo --all-features --no-verify --allow-dirty
          sleep 10
          cargo publish -p blazen-cli --registry forgejo --no-verify --allow-dirty

      - name: Save cargo cache
        if: always() && steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/s3-cache@main
        with:
          mode: save
          key: cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          path: ${{ env.CARGO_HOME }}/registry ${{ env.CARGO_HOME }}/git
          bucket: ${{ secrets.S3_BUCKET }}
          endpoint: https://${{ secrets.S3_ENDPOINT }}
          region: ${{ secrets.S3_REGION }}
          access-key: ${{ secrets.S3_ACCESS_KEY }}
          secret-key: ${{ secrets.S3_ACCESS_SECRET }}

      - name: Mark step complete
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: mark
          version: ${{ needs.version.outputs.version }}
          step: publish-rust
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

  publish-python:
    name: Publish Python Package
    runs-on: ubuntu-latest
    needs: version
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check step status
        id: check
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: check
          version: ${{ needs.version.outputs.version }}
          step: publish-python
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

      - name: Setup Rust
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/setup-rust@main
        with:
          registries: '[{"name": "forgejo", "index": "sparse+https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/cargo/", "token": "${{ secrets.FORGEJO_ACTIONS_TOKEN }}"}]'

      - name: Setup Python
        if: steps.check.outputs.completed != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        if: steps.check.outputs.completed != 'true'
        run: pip install uv

      - name: Install build tools
        if: steps.check.outputs.completed != 'true'
        run: uv pip install --system maturin twine

      - name: Stamp version
        if: steps.check.outputs.completed != 'true'
        run: |
          VERSION="${{ needs.version.outputs.version }}"
          # Update workspace version so maturin picks it up
          sed -i "s/^version = \"0.1.0\"/version = \"$VERSION\"/" Cargo.toml

      - name: Build wheel with maturin
        if: steps.check.outputs.completed != 'true'
        run: maturin build --release -m crates/blazen-py/Cargo.toml

      - name: Publish to Forgejo PyPI
        if: steps.check.outputs.completed != 'true'
        run: |
          twine upload \
            --repository-url https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/pypi \
            -u zachhandley \
            -p ${{ secrets.FORGEJO_ACTIONS_TOKEN }} \
            target/wheels/*.whl

      - name: Mark step complete
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: mark
          version: ${{ needs.version.outputs.version }}
          step: publish-python
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

  publish-node:
    name: Publish Node Package
    runs-on: ubuntu-latest
    needs: version
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check step status
        id: check
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: check
          version: ${{ needs.version.outputs.version }}
          step: publish-node
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

      - name: Setup Rust
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/setup-rust@main
        with:
          registries: '[{"name": "forgejo", "index": "sparse+https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/cargo/", "token": "${{ secrets.FORGEJO_ACTIONS_TOKEN }}"}]'

      - name: Setup Node
        if: steps.check.outputs.completed != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Stamp version
        if: steps.check.outputs.completed != 'true'
        run: |
          VERSION="${{ needs.version.outputs.version }}"
          # Update workspace version
          sed -i "s/^version = \"0.1.0\"/version = \"$VERSION\"/" Cargo.toml
          # Update package.json version
          sed -i "s/\"version\": \"0.1.0\"/\"version\": \"$VERSION\"/" crates/blazen-node/package.json

      - name: Setup pnpm
        if: steps.check.outputs.completed != 'true'
        run: corepack enable && corepack prepare pnpm@latest --activate

      - name: Install dependencies
        if: steps.check.outputs.completed != 'true'
        run: |
          cd crates/blazen-node
          pnpm install

      - name: Build with napi
        if: steps.check.outputs.completed != 'true'
        run: |
          cd crates/blazen-node
          pnpm run build

      - name: Publish to Forgejo npm
        if: steps.check.outputs.completed != 'true'
        run: |
          cd crates/blazen-node
          echo "//forge.blackleafdigital.com/api/packages/BlackLeafDigital/npm/:_authToken=${{ secrets.FORGEJO_ACTIONS_TOKEN }}" > .npmrc
          pnpm publish --registry https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/npm/ --no-git-checks

      - name: Mark step complete
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: mark
          version: ${{ needs.version.outputs.version }}
          step: publish-node
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [version, publish-rust, publish-python, publish-node]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check step status
        id: check
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: check
          version: ${{ needs.version.outputs.version }}
          step: create-release
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}

      - name: Install git-cliff
        if: steps.check.outputs.completed != 'true'
        run: |
          GC_VERSION="2.7.0"
          curl -fsSL "https://github.com/orhun/git-cliff/releases/download/v${GC_VERSION}/git-cliff-${GC_VERSION}-x86_64-unknown-linux-gnu.tar.gz" | tar xz
          sudo mv "git-cliff-${GC_VERSION}/git-cliff" /usr/local/bin/
          git-cliff --version

      - name: Generate changelog
        id: changelog
        if: steps.check.outputs.completed != 'true'
        run: |
          VERSION="v${{ needs.version.outputs.version }}"

          LAST_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            git-cliff "${LAST_TAG}..${VERSION}" --tag "${VERSION}" -o CHANGELOG_RELEASE.md
          else
            git-cliff --tag "${VERSION}" -o CHANGELOG_RELEASE.md
          fi

          cat > RELEASE_NOTES.md << NOTES_EOF
          $(cat CHANGELOG_RELEASE.md)

          ## Installation

          **Rust:**
          \`\`\`bash
          cargo add blazen --registry forgejo
          \`\`\`

          **Python:**
          \`\`\`bash
          uv pip install blazen --index-url https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/pypi/simple/
          \`\`\`

          **Node.js:**
          \`\`\`bash
          pnpm add blazen --registry https://forge.blackleafdigital.com/api/packages/BlackLeafDigital/npm/
          \`\`\`
          NOTES_EOF

      - name: Create Forgejo release
        if: steps.check.outputs.completed != 'true'
        run: |
          VERSION="v${{ needs.version.outputs.version }}"

          curl -s -X POST \
            -H "Authorization: token ${{ secrets.FORGEJO_ACTIONS_TOKEN }}" \
            -H "Content-Type: application/json" \
            "https://forge.blackleafdigital.com/api/v1/repos/${{ github.repository }}/releases" \
            -d "{
              \"tag_name\": \"${VERSION}\",
              \"name\": \"Blazen ${VERSION}\",
              \"body\": $(jq -Rs . < RELEASE_NOTES.md),
              \"draft\": false,
              \"prerelease\": false
            }"

      - name: Mark step complete
        if: steps.check.outputs.completed != 'true'
        uses: https://forge.blackleafdigital.com/Public/actions/ci-step-status@main
        with:
          mode: mark
          version: ${{ needs.version.outputs.version }}
          step: create-release
          base-url: https://forge.blackleafdigital.com
          owner: BlackLeafDigital
          auth: zachhandley:${{ secrets.FORGEJO_ACTIONS_TOKEN }}
